import { CreateComponent, SyncComponent } from '../svg.component';

export const touchSensorCreate: CreateComponent = (state, frame, draw) => {};

export const touchSensorUpdate: SyncComponent = (state, frame, draw) => {};
